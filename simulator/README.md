# Simulator Assets

This directory now stores static assets used by the hosted simulator worker.

- `casephotos/` contains source images uploaded to synthetic cases.

The legacy standalone browser simulator UI files were removed after the hosted
backend console became the primary execution path.
